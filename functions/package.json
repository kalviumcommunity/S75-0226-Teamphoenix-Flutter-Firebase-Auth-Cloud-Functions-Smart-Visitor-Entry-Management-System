{
  "name": "smart-visitor-entry-functions",
  "description": "Cloud Functions for Smart Visitor Entry Management System",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && firebase emulator:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "serve": "npm run build && firebase emulator:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "typescript": "^5.3.3"
  }
}
